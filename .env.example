# Copie para .env: cp .env.example .env

PORT=3000

DB_HOST=seu-host.mysql.database.azure.com
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=ipf_db
DB_PORT=3306

# Mínimo 32 caracteres. Gerar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=
