# Matriz / Arquitetura — /matriz
# Pré-requisito: @token com módulo arquitetura

@url_base = http://localhost:3000
@token =
@pedido_id = 1

### Status da regra de matriz
GET {{url_base}}/matriz/status/{{pedido_id}}
Authorization: Bearer {{token}}
